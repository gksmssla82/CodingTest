#include <bits/stdc++.h>
using namespace std;
int main() {
	  int a;
    string s;
    cin >> a;       
    cin >> s;

	for (auto& i : s)
		{
			if (isupper(i))
				cout << (char)tolower(i);
			else
				cout << (char)toupper(i);
		}

   

    return 0;
}